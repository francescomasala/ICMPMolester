# Copy to lines.toml and adjust to match your broadband circuits.

defaults.ping_count = 5
defaults.ping_timeout_ms = 1000
defaults.traceroute_max_hops = 30
defaults.packet_loss_alert_threshold = 1.0

[[lines]]
name = "Primary FTTH"
target = "8.8.8.8"
ping_count = 8

[[lines]]
name = "Backup LTE"
target = "1.1.1.1"
ping_timeout_ms = 1500
